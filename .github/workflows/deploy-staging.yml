name: Deploy - Staging

on:
  push:
    branches: [ develop ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Staging Server
        run: |
          echo "Deploying staging environment..."
          # Example for Render, Railway, or K8s
          # kubectl set image deployment/nextjs-app nextjs-app=ghcr.io/${{ github.repository }}:${{ github.sha }}
